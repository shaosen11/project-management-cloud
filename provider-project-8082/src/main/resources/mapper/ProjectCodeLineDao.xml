<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.lingnan.dao.ProjectCodeLineDao">

    <resultMap type="com.edu.lingnan.entity.ProjectCodeLine" id="ProjectCodeLineMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="projectsId" column="projects_id" jdbcType="INTEGER"/>
        <result property="codeLineNumber" column="code_line_number" jdbcType="INTEGER"/>
        <result property="uploadTime" column="upload_time" jdbcType="OTHER"/>
        <result property="deleteFlag" column="delete_flag" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ProjectCodeLineMap">
        select
          id, projects_id, code_line_number, upload_time, delete_flag
        from project_management_cloud.project_code_line
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ProjectCodeLineMap">
        select
          id, projects_id, code_line_number, upload_time, delete_flag
        from project_management_cloud.project_code_line
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="ProjectCodeLineMap">
        select
          id, projects_id, code_line_number, upload_time, delete_flag
        from project_management_cloud.project_code_line
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="projectsId != null">
                and projects_id = #{projectsId}
            </if>
            <if test="codeLineNumber != null">
                and code_line_number = #{codeLineNumber}
            </if>
            <if test="uploadTime != null">
                and upload_time = #{uploadTime}
            </if>
            <if test="deleteFlag != null">
                and delete_flag = #{deleteFlag}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into project_management_cloud.project_code_line(projects_id, code_line_number, upload_time, delete_flag)
        values (#{projectsId}, #{codeLineNumber}, #{uploadTime}, #{deleteFlag})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update project_management_cloud.project_code_line
        <set>
            <if test="projectsId != null">
                projects_id = #{projectsId},
            </if>
            <if test="codeLineNumber != null">
                code_line_number = #{codeLineNumber},
            </if>
            <if test="uploadTime != null">
                upload_time = #{uploadTime},
            </if>
            <if test="deleteFlag != null">
                delete_flag = #{deleteFlag},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from project_management_cloud.project_code_line where id = #{id}
    </delete>

</mapper>