<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.lingnan.dao.ProjectMessageNeedToDoRelationshipDao">

    <resultMap type="com.edu.lingnan.entity.ProjectMessageNeedToDoRelationship" id="ProjectMessageNeedToDoRelationshipMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="projectMessageId" column="project_message_id" jdbcType="INTEGER"/>
        <result property="documentId" column="document_id" jdbcType="INTEGER"/>
        <result property="deleteFlag" column="delete_flag" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ProjectMessageNeedToDoRelationshipMap">
        select
          id, project_message_id, document_id, delete_flag
        from project_management_cloud.project_message_need_to_do_relationship
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ProjectMessageNeedToDoRelationshipMap">
        select
          id, project_message_id, document_id, delete_flag
        from project_management_cloud.project_message_need_to_do_relationship
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="ProjectMessageNeedToDoRelationshipMap">
        select
          id, project_message_id, document_id, delete_flag
        from project_management_cloud.project_message_need_to_do_relationship
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="projectMessageId != null">
                and project_message_id = #{projectMessageId}
            </if>
            <if test="documentId != null">
                and document_id = #{documentId}
            </if>
            <if test="deleteFlag != null">
                and delete_flag = #{deleteFlag}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into project_management_cloud.project_message_need_to_do_relationship(project_message_id, document_id, delete_flag)
        values (#{projectMessageId}, #{documentId}, #{deleteFlag})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update project_management_cloud.project_message_need_to_do_relationship
        <set>
            <if test="projectMessageId != null">
                project_message_id = #{projectMessageId},
            </if>
            <if test="documentId != null">
                document_id = #{documentId},
            </if>
            <if test="deleteFlag != null">
                delete_flag = #{deleteFlag},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from project_management_cloud.project_message_need_to_do_relationship where id = #{id}
    </delete>

</mapper>