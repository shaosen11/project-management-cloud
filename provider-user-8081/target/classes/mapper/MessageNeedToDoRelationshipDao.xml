<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.lingnan.dao.MessageNeedToDoRelationshipDao">

    <resultMap type="com.edu.lingnan.entity.MessageNeedToDoRelationship" id="MessageNeedToDoRelationshipMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="messageId" column="message_id" jdbcType="INTEGER"/>
        <result property="projectsUserCooperrationId" column="projects_user_cooperration_id" jdbcType="INTEGER"/>
        <result property="deleteFlag" column="delete_flag" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="MessageNeedToDoRelationshipMap">
        select
          id, message_id, projects_user_cooperration_id, delete_flag
        from project_management_user_cloud.message_need_to_do_relationship
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MessageNeedToDoRelationshipMap">
        select
          id, message_id, projects_user_cooperration_id, delete_flag
        from project_management_user_cloud.message_need_to_do_relationship
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="MessageNeedToDoRelationshipMap">
        select
          id, message_id, projects_user_cooperration_id, delete_flag
        from project_management_user_cloud.message_need_to_do_relationship
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="messageId != null">
                and message_id = #{messageId}
            </if>
            <if test="projectsUserCooperrationId != null">
                and projects_user_cooperration_id = #{projectsUserCooperrationId}
            </if>
            <if test="deleteFlag != null">
                and delete_flag = #{deleteFlag}
            </if>
        </where>
    </select>


</mapper>